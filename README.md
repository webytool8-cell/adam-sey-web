# Adam Sey Web

Next.js 15 studio website foundation with public portfolio routes and owner-only Supabase auth.

## Setup

1. `npm install`
2. Add `.env.local` with:
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
3. `npm run dev`
